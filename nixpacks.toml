[phases.setup]
# ใช้เพียง nodejs; npm มาพร้อมกับ node อยู่แล้วใน Nix
nixPkgs = ['nodejs']

[phases.install]
cmds = ['npm ci --no-audit --no-fund']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npm start'
